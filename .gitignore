# Secrets — never commit Cloudbeds credentials
.env
.env.local
.env*.local

# Node / Next.js (for the build phase)
node_modules/
.next/
out/
dist/
npm-debug.log*
next-env.d.ts
*.tsbuildinfo

# OS / editor
.DS_Store
*.log
.vscode/
.idea/
.vercel

# Snowflake data-share schema dumps (local exploration; may contain business addresses)
.snowflake-discovery/

# Local input artifacts / stray screenshots (not versioned)
/*.pdf
/*.png
outputs/EliseDataAccess_Email_070226.md

# Monica's raw daily occupancy workbooks — 869 files / ~2.0 GB of source .xlsx
# snapshots kept locally for reconciliation. Far too large for git; the derived
# outputs (reconciliation + methodology in outputs/) are versioned instead.
/Occupancy Report/

# Python bytecode from scripts/
__pycache__/

# Never commit the MCP connector URL - the secret IS the credential.
.secrets/
