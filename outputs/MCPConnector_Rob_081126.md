Rob — you can now ask Claude Desktop about the business directly.

This connects Claude to the **whole Stayable Operating Dashboard**, not just
Cloudbeds: occupancy, ADR, RevPAR and revenue by property or portfolio, the
daily revenue report, today's arrivals and out-of-order rooms, plus evictions,
the contractor schedule, 1-star reviews and the EliseAI leasing funnel.

It runs on the same code as the dashboard, so the numbers match the page.

**Setup (one time):** Claude Desktop → Settings → Connectors → Add custom connector

- Name: `Stayable Dashboard`
- Remote MCP server URL: *(sent separately)*
- Leave the OAuth fields empty.

It's read-only, and there are no guest details in it. **Treat the URL like a
password — please don't forward it.** If it stops working, tell me; it usually
means the key was rotated and you need a new link.
